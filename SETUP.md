# 後端設定（每日測驗成績自動記錄）

網站本身是純前端，直接放上 GitHub Pages 就能用。
若要讓同仁每天作答完自動把成績寫進 Google 試算表，照下面做。

## 你目前的狀況

鑑別測驗網站已經有一組在運作的 Apps Script 後端，這次的每日測驗**沿用同一組**，
所以不用重開專案、不用換網址，只要把後端程式更新成 v2（多了每日測驗的路由）。

## 步驟 1：更新 Apps Script（約 3 分鐘）

1. 打開你放鑑別測驗結果的那份 Google 試算表
2. 上方選單「擴充功能」→「Apps Script」
3. 把編輯器裡的舊程式碼**全部刪除**
4. 打開本資料夾的 `apps-script-backend.gs`，全部複製貼進去
5. Ctrl+S 儲存

> v2 是往前相容的：payload 沒有 `type` 或 `type` 是 `diagnostic` 時，寫入原本的「測驗結果」工作表，行為完全不變。
> 鑑別測驗網站**不需要改任何東西**。

## 步驟 2：部署新版本（約 2 分鐘）

1. 右上角「部署」→「管理部署作業」
2. 點現有部署右邊的鉛筆圖示（編輯）
3. 「版本」下拉選「**新版本**」
4. 點「部署」

**網址不會變**，所以 `assets/config.js` 不用動。

如果你是第一次部署（沒有現有部署作業），改成：
「部署」→「新增部署作業」→ 類型選「網頁應用程式」→
執行身分：**我**、誰可以存取：**所有人** → 部署 → 複製網址 →
貼到 `assets/config.js` 的 `GAS_WEB_APP_URL`。

## 步驟 3：確認有作用

1. 打開網站的 Day 1，填姓名、隨便作答完六題、送出
2. 送出後下方應該顯示「✓ 已完成，成績已記錄」
3. 回到試算表，會多出一個「**每日測驗結果**」工作表，並有一列資料

「每日測驗結果」的欄位：

| 提交時間 | 姓名 | 週次 | 天數 | 主題 | 得分(%) | 答對題數 | 總題數 | 答錯題號 | 作答明細(JSON) |
|---|---|---|---|---|---|---|---|---|---|

## 步驟 4（選用）：每日達成率摘要

v2 多附了一個小工具。重新整理試算表後，上方選單會多出「**每日測驗**」→
「**產生每日達成率摘要**」，點下去會產生／更新「每日達成率」工作表：

| 週次/天數 | 主題 | 作答人數 | 平均分數 | 未達 70 分人數 |
|---|---|---|---|---|

同一個人重複作答只算最後一次。週日彙整進度時直接點這個就好。

## 沒有設定後端會怎樣？

網站照常運作，同仁一樣看得到解析與分數，只是成績只留在自己的瀏覽器、不會回傳。
畫面會顯示「（本站尚未啟用自動記錄，成績只留在這台裝置）」。

## 隱私提醒

- 試算表會記錄每位同仁的姓名與每日分數，**請把試算表共用權限設為僅限召集人／管理員可檢視**
- 網站本身不做身分驗證，姓名是同仁自己輸入的，別放在對外公開的地方宣傳
- 若同仁對姓名記錄有疑慮，可改用員編或暱稱作答（記得先在群組公告統一規則）

## 疑難排解

**送出後顯示「成績回傳失敗」**
- 多半是 Apps Script 沒有部署成「所有人可存取」。回到「管理部署作業」確認一次。
- 同仁可以按畫面上的「重新回傳成績」再試，作答結果不會不見。

**試算表沒有出現新的一列**
- 確認 `assets/config.js` 的網址結尾是 `/exec`（不是 `/dev`）
- 確認部署時選的是「新版本」而不是沿用舊版本

**同仁說「我做過了但打開又要重做」**
- 進度存在瀏覽器的 localStorage，換裝置、換瀏覽器、開無痕都會不見。
  這是正常的——成績已經回傳到試算表了，以試算表為準。
