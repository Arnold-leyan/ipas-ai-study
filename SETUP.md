# 後端設定（每日測驗成績自動記錄）

網站本身是純前端，直接放上 GitHub Pages 就能用。
若要讓同仁作答完自動把成績寫進 Google 試算表，照下面做一次，約 15 分鐘。

> **這是讀書會網站自己的後端**，跟鑑別測驗那份 Apps Script 是分開的兩套。
> 鑑別測驗的資料留在原本那份試算表，不受影響，也不要去改它。
> 之後讀書會要加的功能（W2、模擬考、線上打卡…）都往這一份加。

## 步驟 1：建立新的 Google 試算表

1. 到 [Google 試算表](https://sheets.google.com) 新增一份空白試算表
2. 檔名改成「**AI應用規劃師 讀書會紀錄**」（或你喜歡的名稱）

工作表不用自己建，後端第一次寫入時會自動建好並加上標題列。

## 步驟 2：貼上程式碼

1. 上方選單「擴充功能」→「Apps Script」，會開一個新分頁
2. 把預設的 `function myFunction() {}` 整段刪掉
3. 打開本資料夾的 `apps-script-backend.gs`，全部複製貼進去
4. Ctrl+S 儲存，專案取名例如「讀書會網站後端」

## 步驟 3：部署為網頁應用程式

1. 右上角「部署」→「新增部署作業」
2. 類型（齒輪圖示）選「**網頁應用程式**」
3. 設定：
   - **執行身分**：我（你自己的 Google 帳號）
   - **誰可以存取**：**所有人**
4. 點「部署」
5. 第一次會要求授權：選你的 Google 帳號 → 出現「未經驗證」警告是正常的 →
   點「進階」→「前往（專案名稱）」→ 允許
6. 複製顯示出來的網址，格式像這樣：
   ```
   https://script.google.com/macros/s/AKfycb....../exec
   ```
   **結尾必須是 `/exec`**，不是 `/dev`。

## 步驟 4：貼回網站

打開 `assets/config.js`，把網址填進去：

```js
var GAS_WEB_APP_URL = 'https://script.google.com/macros/s/AKfycb....../exec';
```

存檔後推上 GitHub：

```bash
git add assets/config.js
git commit -m "設定後端網址"
git push
```

GitHub Pages 大約一分鐘後生效。

## 步驟 5：確認有作用

**方法 A（最快，不用開網站）**
回到試算表，重新整理一次頁面，上方選單會多出「**讀書會**」→
「**測試後端是否正常**」。點下去，會寫入一列姓名為「（測試）」的資料並跳出結果。
確認完記得把那一列刪掉。

**方法 B（完整流程）**
打開網站的 Day 1，填姓名、作答完六題送出。
畫面下方應該顯示「✓ 已完成，成績已記錄」，試算表也會多一列。

「每日測驗結果」的欄位：

| 提交時間 | 姓名 | 週次 | 天數 | 主題 | 得分(%) | 答對題數 | 總題數 | 答錯題號 | 作答明細(JSON) |
|---|---|---|---|---|---|---|---|---|---|

## 每週彙整進度

試算表選單「讀書會」→「**產生每日達成率摘要**」，會產生／更新「每日達成率」工作表：

| 週次/天數 | 主題 | 作答人數 | 平均分數 | 未達 70 分人數 | 未達 70 分名單 |
|---|---|---|---|---|---|

同一個人重複作答只算最後一次。週日彙整直接點這個就好。

## 之後要擴充時

`apps-script-backend.gs` 裡**只有最上面的 `SPECS` 需要改**。
每一種要記錄的資料就是 `SPECS` 裡的一個項目，寫三件事：寫到哪個工作表、
欄位名稱、怎麼把送過來的資料轉成一列。

新增類型不用改 `doPost`。檔案最下方附了一個「週五打卡」的現成範例，
需要時把註解拿掉、搬進 `SPECS` 即可。

**改完程式碼一定要重新部署**：
「部署」→「管理部署作業」→ 鉛筆圖示 → 版本選「**新版本**」→ 部署。
用這個方式網址不會變，`config.js` 不用再動。

> 只按 Ctrl+S 儲存是**不會**生效的，網頁應用程式跑的是「已部署的版本」。
> 這是最常見的卡關點。

## 沒有設定後端會怎樣？

網站照常運作，同仁一樣看得到解析與分數，只是成績只留在自己的瀏覽器。
畫面會顯示「（本站尚未啟用自動記錄，成績只留在這台裝置）」。

## 隱私提醒

- 試算表會記錄每位同仁的姓名與每日分數，**請把試算表共用權限設為僅限召集人／管理員可檢視**
- 後端的 `doGet` 刻意不回傳任何成績資料——這個網址是「所有人可存取」，
  回傳資料等於公開全班分數。要加查詢功能前請先想清楚這點。
- 網站本身不做身分驗證，姓名是同仁自己輸入的
- 若同仁對姓名記錄有疑慮，可改用員編或暱稱（記得先在群組公告統一規則）

## 疑難排解

**送出後顯示「⚠ 成績回傳失敗」**
- 多半是部署時「誰可以存取」沒選「所有人」。回「管理部署作業」確認。
- 或是 `config.js` 的網址結尾是 `/dev` 而不是 `/exec`。
- 同仁可以按畫面上的「重新回傳成績」再試，作答結果不會不見。

**改了程式碼但沒有效果**
- 你只儲存、沒有部署新版本。見上面「之後要擴充時」。

**試算表沒出現新的一列**
- 先用選單的「測試後端是否正常」確認後端本身有沒有問題，
  可以縮小範圍是後端還是前端網址設定的問題。

**同仁說「我做過了但打開又要重做」**
- 進度存在瀏覽器 localStorage，換裝置、換瀏覽器、開無痕都會不見。
  這是正常的——成績已經回傳到試算表，以試算表為準。
